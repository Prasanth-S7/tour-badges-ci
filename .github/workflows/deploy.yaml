name: Deploy Worker

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to Cloudflare Workers
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: |
          pnpm install
          ls

      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy src/index.ts --env production
          environment: |
            {
              "env": {
                "production": {
                  "vars": {
                    "ENVIRONMENT": "production",
                    "ALLOWED_ORIGINS": "https://tour.json-schema.org",
                    "BADGR_API": "https://api.badgr.io"
                  },
                  "d1_databases": [
                    {
                      "binding": "DB",
                      "database_name": "db",
                      "database_id": "${{ secrets.PROD_DATABASE_ID }}"
                    }
                  ]
                }
              }
            }
      - name: Debug Wrangler Config
        run: |
          echo "Current directory:"
          ls -la
          
          echo "Original wrangler.jsonc:"
          cat wrangler.jsonc || echo "No wrangler.jsonc found"
          
          echo "Merged configuration:"
          pnpm exec wrangler deploy src/index.ts --env production --dry-run --outdir=./deploy-config
          cat ./deploy-config/wrangler.json
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}